# Filtered at build time: each at-delimited placeholder below is replaced
# by curie (project version, git commit, and the inline api.url property).
version=@project.version@
build.commit=@git.commit.id@
api.url=@api.url@
