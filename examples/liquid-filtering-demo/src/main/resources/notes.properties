# This file lives in src/main/resources (not src/main/config), so it is
# NOT processed by the Liquid filter stage and its template syntax stays verbatim.
note=This file is not filtered: {{ project.version }} stays literal.